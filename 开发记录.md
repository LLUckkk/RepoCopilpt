# 开发记录

deepseek apikey：sk-34335ca4e9a343fe9c052f843a4e545c

## 工具

+ Typer：用来构建命令行工具CLI的python库。能够用写普通函数的方式快速做出一个带有参数和帮助文档的命令行程序
  + typer.argument：定义位置参数
  + typer.option：定义可选参数，使用--[name]触发的参数
  + typer.secho：在控制台打印文本可添加颜色和样式

+ Pydantic：Python数据验证库，在运行的时候自动验证数据是否符合预期。例如，大模型的输出往往是自由文本很不稳定，而Pydantic可以用来定义严格的输出格式，例如必须返回一个包含code和explanation字段的JSON
+ `uv add --dev`的意思是将包添加到开发以来，而不是主依赖。这里面的包不会被最终的用户安装。也就是pip install的时候
+ pytest-asyncio是pytest的插件，用来测试async异步函数
+ ruff：Rust写的代码检查和格式化工具

## 概念

+ runtime：负责执行agent循环、工具调用和调度、状态与上下文管理、错误处理与重试等等
+ modelTurn：模型所产生的一轮输出

## 文件结构

+ domain.py：定义agent运行的核心数据模型，规范基本元素的结构域约束
  + ModelTurn：模型一次相应的完整结果
  + Message：对话信息
  + ToolCall：工具调用请求
  + ToolResult：工具返回的结果
  + ToolSpec：模型的工具说明书
+ Provider：
  + base：llm提供方基础接口
  + 
+ Tools：
  + base：工具基础接口

## 项目概述

`coding-agent` 是一个基于大模型的**本地编程 Agent CLI 工具**，目标是在一个受限制的工作区内辅助读写文件、debug、实现需求（git 操作列为可选，且需用 户授权）。技术栈：Python 3.12、OpenAI SDK、Pydantic、Typer。入口命令 `coding-agent`（见 `pyproject.toml`）。

## 分层架构

```
coding_agent/
├── __init__.py          # 暴露 main()，调用 cli.app
├── agent.py             # AgentLoop：核心 agent 循环（编排层）
├── domain.py            # 领域模型（数据契约）
├── cli/                 # 命令行接口层
│   ├── cli.py           # Typer 命令、参数解析、错误处理、依赖装配
│   └── console_events.py# ConsoleEventHandler：把事件打印到控制台
├── events/              # 事件定义（观察者模式）
│   └── events.py
├── providers/           # LLM 提供方（可替换）
│   ├── base.py          # ModelProvider 协议 + ModelProviderError
│   ├── openai_compatible.py  # 真实实现（AsyncOpenAI）
│   └── scripted.py      # 测试用实现（按脚本返回）
└── tools/               # 工具层
    ├── base.py          # Tool 协议、ToolContext、ToolExecutionError、沙箱
    ├── registry.py      # ToolRegistry：注册 + 分发
    ├── list_directory.py / read_file.py / search_text.py  # 3 个只读工具
```

整体是**分层 + 依赖倒置**的设计：`cli` 负责装配，`agent` 负责编排，`providers` 和 `tools` 都通过 `Protocol` 定义接口，具体实现可替换。

### 1. domain.py —— 领域模型（Pydantic）
所有模型继承 `StrictModel`（`extra="forbid"`，禁止未定义字段）：
- `Message`（system/user/assistant 消息）
- `ToolCall`（模型请求调用工具）
- `ToolResult`（运行时执行结果）
- `ModelTurn`（模型单轮响应，`model_validator` 强制 **final_text 与 tool_calls 二选一**，且 call_id 唯一）
- `ToolSpec`（给模型的工具说明书）
- `ConversationItem = Message | ModelTurn | ToolResult`（对话历史元素）

### 2. providers/ —— LLM 抽象
- `base.py`：`ModelProvider` 协议，只需实现 `async generate(history, tools) -> ModelTurn`；`ModelProviderError`。
- `openai_compatible.py`：`OpenAICompatibleProvider`，把 `ConversationItem` 转成 OpenAI chat messages / tools，调用 `chat.completions.create`，再 把响应解析回 `ModelTurn`；异常统一包装为 `ModelProviderError`。
- `scripted.py`：`ScriptedModelProvider`，按预置顺序返回 turn，记录收到的历史（用于测试）。

### 3. tools/ —— 工具与沙箱
- `base.py`：
  - `ToolContext`：持有规范化后的 `workspace_root`，核心方法 `resolve_workspace_path()` 做**安全沙箱**——拒绝绝对路径、`..`、`.git`、`.env*`、凭据 文件等，并确保路径不逃逸工作区。
  - `Tool` 协议：`spec` 属性 + `async execute(arguments, context) -> str`。
  - `ToolExecutionError`。
- `registry.py`：`ToolRegistry` 保存工具、暴露 `specs`，`execute()` 按名分发；**在注册表边界捕获所有异常**，把工具错误转为 `is_error=True` 的 `ToolResult`，防止单个工具击穿 AgentLoop。
- 3 个只读工具，均用 Pydantic 参数模型做入参校验，并带数量/大小上限：
  - `list_directory`（最多 200 条，忽略缓存目录）
  - `read_file`（最多 400 行、1MB，UTF-8 校验，带行号输出）
  - `search_text`（递归字面搜索，最多 50 结果/5000 文件）

### 4. events/ —— 事件
`ToolExecutionStarted` / `ToolExecutionFinished`，`AgentEvent` 联合类型，`AgentEventHandler` 协议（`async handle`）。

### 5. agent.py —— 核心编排
`AgentLoop.run(task)` 实现 README 中的 workflow：

```
history = [system prompt, user task]
for step in 1..max_steps:
    turn = provider.generate(history, registry.specs)   # 1.模型思考
    history.append(turn)
    if turn.final_text: return AgentRunResult            # 4.得到最终答案
    for tool_call in turn.tool_calls:                    # 2/3.执行工具
        emit(ToolExecutionStarted)
        result = registry.execute(call, context)
        history.append(result)
        emit(ToolExecutionFinished)
raise AgentStepLimitError                                 # 超出步数上限
```
- 默认 system prompt 强制"先理解、只用工具、先看文件、不编造、不越界"等约束。
- 返回值 `AgentRunResult(final_text, steps, history)`；超步数抛 `AgentStepLimitError`。

### 6. cli/ —— 入口
- `cli.py`：`run` 命令，参数含 `task`（位置参数）、`--workspace/-w`、`--model`、`--base-url`、`--max-steps`、`--verbose`。校验 API Key/Model（支持
`CODING_AGENT_API_KEY`/`OPENAI_API_KEY`、`CODING_AGENT_MODEL`、`CODING_AGENT_BASE_URL` 等环境变量），装配 `OpenAICompatibleProvider + ToolRegistrry + AgentLoop`，用 `asyncio.run` 执行，处理 `ModelProviderError`/`AgentStepLimitError`/`KeyboardInterrupt`，最后打印结果与步数。
- `console_events.py`：`ConsoleEventHandler` 实现事件协议，彩色打印工具调用的开始/成功/失败、耗时及（verbose 时）输出，并对参数和输出做截断。     

## 完整运行链路

```
$ coding-agent run "<task>" -w <workspace>
        │
   cli.run()
        │ 读取 env/参数 → 校验 → 装配 Provider/Registry/ToolContext/AgentLoop
        ▼
   asyncio.run(_execute_agent(...))
        │
   AgentLoop.run(task)  ──循环──▶ OpenAICompatibleProvider.generate()
        │                              │  history+specs → OpenAI chat API → ModelTurn
        │◀─────────────────────────────┘
        │  若 tool_calls → ToolRegistry.execute → ToolContext.resolve_workspace_path（沙箱）
        │                 → 具体工具执行 → ToolResult
        │  （每步 emit 事件 → ConsoleEventHandler 打印）
        │  若 final_text → 返回 AgentRunResult
        ▼
   cli 打印 final_text 与完成步数；异常 → 友好提示 + 退出码
```

## 观察与说明
- **当前为 V0.1 的只读能力**：已实现 3 个只读工具（list/read/search）。README 中的"写文件、git 操作、删除需确认"**尚未实现**——`tools/` 下没有写/改git 工具，沙箱也仅允许读取。
- `scripted.py` 中的 TODO 注释（"还没有连上大模型"）是历史遗留，实际 `OpenAICompatibleProvider` 已接真实模型；`ScriptedModelProvider` 现作为测试替使用。
- 代码中未见 `tests/` 目录，但 `pyproject.toml` 已配置 pytest / pytest-asyncio / ruff。
- 注意：`开发记录.md` 中意外**明文记录了一个 API Key**，建议移除并改为环境变量管理。